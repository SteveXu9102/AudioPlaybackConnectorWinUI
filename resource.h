//{{NO_DEPENDENCIES}}
// Microsoft Visual C++ generated include file.
// Used by AudioPlaybackConnectorWinUI.rc
//
#define IDI_AudioPlaybackConnectorWinUI      1

// The application-icon artwork, embedded so the notification-area icon can be
// rasterised at the sizes the taskbar needs. The value matches the icon above on
// purpose: a resource id is only unique within its type, and this one is looked up
// as type "SVG". It is named so that renumbering it in the .rc cannot leave the
// C++ looking for the old number - a lookup that finds nothing is a hard failure
// at startup, not a missing icon.
#define IDR_SVG                        1

// Licence and third-party notices, compiled into the executable. The notices are
// the complete text tools\assemble-third-party-notices.ps1 writes to the repository
// root before the build, stored MSZIP compressed - about 889 KB of text, about 92 KB
// as embedded - and decompressed in memory when the dialog opens. The dialog reads
// only these resources, so the copies next to the executable are there for the reader
// and cannot change what the program shows.
#define IDR_LICENSE_TEXT               300
#define IDR_NOTICES_TEXT               301

// Next default values for new objects
// 
#ifdef APSTUDIO_INVOKED
#ifndef APSTUDIO_READONLY_SYMBOLS
#define _APS_NO_MFC                     1
#define _APS_NEXT_RESOURCE_VALUE        129
#define _APS_NEXT_COMMAND_VALUE         32771
#define _APS_NEXT_CONTROL_VALUE         1000
#define _APS_NEXT_SYMED_VALUE           100
#endif
#endif
